#include "prim.h"
#include <iostream>
#include <limits>

using namespace std;

//Constructor to Initialize the graph
Graph::Graph(int V)
{
	
}


// Method to add an edge into the graph
void Graph::addEdge(int u, int v, int w)
{
	
}


// The method that constructs the MST from the Graph. Takes starting node as input
void Graph::primMST(int startNode)
{
	
}


// Prints the Edges of the MST. It should return the weight of the MST back to main.
//Look at one of the output.txt on how the edges should be printed.
int Graph::dumpMST() const
{
    return 0; //dummy return
}
